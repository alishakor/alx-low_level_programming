#include <stdio.h>

 /**
   * main - Entry point
   * Description: 'The program prints a string followed by a new line'
   * Return: Always 0
   */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
